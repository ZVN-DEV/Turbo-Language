[package]
name = "task-agent"
version = "0.1.0"
edition = "2026"
description = "Task management API with an AI agent assistant"
license = "MIT"

[dependencies]
turbo-db = "0.1"

[dev-dependencies]
turbo-test-utils = "0.1"

[build]
target = "native"
opt-level = "release"

[agent]
default-model = "claude-sonnet"
