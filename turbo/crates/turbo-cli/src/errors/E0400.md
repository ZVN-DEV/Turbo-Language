# E0400 — Internal codegen error

## What it means
The code generator hit an internal invariant violation. This typically
means the compiler itself has a bug — the semantic analyzer let
through something codegen wasn't prepared to handle.

## How to fix
Please file an issue on the Turbo repository with the source file that
triggered the error. As a workaround, you may be able to refactor the
offending expression.
