# E0505 — Test function has return type

## What it means
Test functions must not declare a return type. The test runner
ignores return values.

## Example
```turbo
test fn my_test() -> i64 {  // return type not allowed
    42
}
```

## How to fix
Remove the return type. Use `assert(...)` to signal test failures.
