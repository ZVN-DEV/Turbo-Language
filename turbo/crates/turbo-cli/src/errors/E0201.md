# E0201 — Match expression has no arms

## What it means
A `match` expression was written with an empty body. Empty matches are
disallowed because they can never produce a value.

## Example
```turbo
fn main() {
    let x = 5
    match x { }
}
```

## How to fix
Add at least one arm, such as a wildcard arm: `_ => ()`.
