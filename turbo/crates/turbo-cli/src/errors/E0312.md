# E0312 — Agent already defined (duplicate)

## What it means
Two `agent` blocks share the same name.

## Example
```turbo
agent Assistant { }
agent Assistant { }  // duplicate
```

## How to fix
Rename one of the agents.
