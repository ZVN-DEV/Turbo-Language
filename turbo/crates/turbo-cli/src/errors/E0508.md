# E0508 — Continue outside loop

## What it means
A `continue` statement appears outside any enclosing `while` or `for`
loop.

## Example
```turbo
fn main() {
    continue  // no surrounding loop
}
```

## How to fix
Move the `continue` inside a loop, or remove it.
