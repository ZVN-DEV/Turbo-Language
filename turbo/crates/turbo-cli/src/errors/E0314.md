# E0314 — No main function found

## What it means
An executable Turbo program must define a `fn main()` entry point.
The compiler couldn't find one in the source.

## Example
```turbo
fn greet() { print("hi") }  // no main
```

## How to fix
Add `fn main() { ... }` at the top level of the file.
