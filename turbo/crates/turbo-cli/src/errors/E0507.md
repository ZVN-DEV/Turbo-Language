# E0507 — Break outside loop

## What it means
A `break` statement appears outside any enclosing `while` or `for`
loop.

## Example
```turbo
fn main() {
    break  // no surrounding loop
}
```

## How to fix
Move the `break` inside a loop, or remove it.
