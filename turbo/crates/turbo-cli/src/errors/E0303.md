# E0303 — Undefined enum

## What it means
An enum was referenced by name but no enum with that name is in scope.

## Example
```turbo
fn main() {
    let c = Color::Red  // Color enum not defined
}
```

## How to fix
Define the enum (`enum Color { Red, Green, Blue }`) before using it,
or import it.
