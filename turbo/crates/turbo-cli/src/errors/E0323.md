# E0323 — Unknown type in trait method

## What it means
A trait method signature referenced a type that the sema couldn't
resolve.

## Example
```turbo
trait Display { fn show(&self) -> Widget }  // Widget not defined
```

## How to fix
Define the type, import it, or use a resolvable type name.
