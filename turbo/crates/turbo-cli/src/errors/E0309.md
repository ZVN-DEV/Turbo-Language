# E0309 — Trait already defined (duplicate)

## What it means
Two traits with the same name were defined in the same module.

## Example
```turbo
trait Display { fn show(&self) -> str }
trait Display { }  // duplicate
```

## How to fix
Rename one of the traits.
