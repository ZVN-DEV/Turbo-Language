# E0304 — Undefined trait

## What it means
A trait was referenced in an `impl` or bound position but hasn't been
defined in scope.

## Example
```turbo
impl Display for Point { }  // Display trait not defined
```

## How to fix
Define the trait before implementing it, or import it from the module
that defines it.
