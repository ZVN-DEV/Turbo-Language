# E0511 — Agent field initializer error

## What it means
An `agent` block's field initializer was invalid: wrong type, unknown
field, or invalid literal.

## Example
```turbo
agent Bot {
    model: 42  // expected str
}
```

## How to fix
Check the expected types for agent fields and provide matching
values.
