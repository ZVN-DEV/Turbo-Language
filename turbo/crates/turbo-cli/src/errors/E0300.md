# E0300 — Undefined variable

## What it means
A variable was referenced that has not been defined in the current
scope or any enclosing scope.

## Example
```turbo
fn main() {
    print(x)  // x is not defined
}
```

## How to fix
Define the variable with `let` before using it, or check for typos in
the variable name.
