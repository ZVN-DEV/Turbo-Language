# E0307 — Enum already defined (duplicate)

## What it means
Two enums with the same name were declared in the same module. Enum
names must be unique.

## Example
```turbo
enum Color { Red }
enum Color { Blue }  // duplicate
```

## How to fix
Rename one of the enums, or merge the variants into a single enum.
