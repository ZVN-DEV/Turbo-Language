[package]
name = "turbo-sema"
version = "0.3.2"
edition = "2021"

[dependencies]
turbo-ast = { path = "../turbo-ast" }

[dev-dependencies]
turbo-lexer = { path = "../turbo-lexer" }
turbo-parser = { path = "../turbo-parser" }
