[package]
name = "turbo-formatter"
version = "0.10.3"
edition = "2021"
rust-version.workspace = true
description.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
turbo-lexer = { version = "0.10.3", path = "../turbo-lexer" }
turbo-parser = { version = "0.10.3", path = "../turbo-parser" }
turbo-ast = { version = "0.10.3", path = "../turbo-ast" }

[dev-dependencies]
# Matches the version pinned by turbo-parser's proptest suite.
proptest = "1.4"
