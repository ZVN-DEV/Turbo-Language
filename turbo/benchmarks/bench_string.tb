// Benchmark: String concatenation
// Measures: string allocation, concatenation overhead
// Expected output: 10000

fn main() {
    let mut s = ""
    let mut i = 0
    while i < 10000 {
        s = s + "x"
        i += 1
    }
    print(len(s))
}
