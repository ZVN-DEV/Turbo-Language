fn idg<T>(x: T) -> T {
    x
}

fn churn(n: i64) -> i64 {
    let mut total = 0
    let mut i = 0
    while i < n {
        let r = idg("x" + to_str(i))
        total = total + len(r)
        i = i + 1
    }
    total
}

fn main() {
    print(churn(1000000))
}
