// Regression: the range upper bound must accept a full binary expression
// (`1..10 + 1`). Previously only a unary was parsed, leaving `+ 1` dangling
// and failing the parse.
fn main() {
    let mut sum = 0
    for i in 1..10 + 1 {
        sum = sum + i
    }
    print(sum)
}
