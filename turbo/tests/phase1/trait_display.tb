struct Point { x: i64, y: i64 }

impl Display for Point {
    fn to_string(self) -> str {
        "({self.x}, {self.y})"
    }
}

fn main() {
    let p = Point { x: 3, y: 4 }
    print(p)
    print("point = {p}")
}
