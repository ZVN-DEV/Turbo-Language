fn apply(f: fn(i64) -> i64, x: i64) -> i64 {
    f(x)
}

fn make_adder(n: i64) -> fn(i64) -> i64 {
    |x: i64| -> i64 { x + n }
}

fn main() {
    let add5 = make_adder(5)
    print(apply(add5, 10))
    let add100 = make_adder(100)
    print(apply(add100, 7))
}
